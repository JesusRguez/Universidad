#include <iostream>

using namespace std;
//Transformar lo que hemos leido del fichero (que se encontraría en una pila) en enteros (código del enunciado de la práctica) y luego terner
//en cuenta que si sumamos y nos llevamos una, hay que sumársela luego a la hora de volverlo a dejar en el fichero.

int main()
{
    cout << "Hello world!" << endl;
    return 0;
}
