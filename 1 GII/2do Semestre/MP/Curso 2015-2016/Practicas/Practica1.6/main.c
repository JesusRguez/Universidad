#include <stdio.h>
#include <stdlib.h>

int escalera (int);

int main()
{
    int n,a[10]={1,2,3,4,5,6,6,7,8,9};

    n=escalera(a);

    printf("La longitud de la escalera mas larga es de &d",n);

    return 0;
}

//Cabecera: int ecalera(int)
//Precondición: el vector "a" ha de estar inicializado
//Postcondición: Calcula el valor de la escalera de numeros más larga en el vector
int escalera (int a)
{
    int i,s;
    s=0;
    i=0;
    if (a([i]+1)==a[i+1])
        return
}
