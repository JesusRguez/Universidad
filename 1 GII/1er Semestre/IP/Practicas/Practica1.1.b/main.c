#include <stdio.h>
#include <stdlib.h>
int main()
{
char letra;
letra = 'b'; //Se le asigna b a letra
printf ("Esta es %d or %c or %x.", letra, letra, letra); //Con %d obtendremos un número entero con signo en base decimal; con %c obtendremos un carácter individual; con %x obtendremos un número entero con signo en base individual.
return 0;                                                //con %x obtendremos un número entero con signo en base individual.
}
